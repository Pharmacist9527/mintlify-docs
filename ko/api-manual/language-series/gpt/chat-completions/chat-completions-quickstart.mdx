---
title: "GPT 전체 모델 인터페이스 - Chat Completions 빠른 시작"
openapi: '/ko/api-manual/language-series/gpt/chat-completions/chat-completions-quickstart.json POST /v1/chat/completions'
---

<Note>
**BaseURL**: 기본 BaseURL은 `https://direct.evolink.ai`이며, 텍스트 모델 지원이 더 우수하고 장시간 연결을 지원합니다. `https://api.evolink.ai`는 멀티모달 서비스의 주력 엔드포인트이며, 텍스트 모델에 대해서는 대체 주소로 사용됩니다.
</Note>

<Note>
**GPT-6**: 이 엔드포인트에서 Sol / Luna로 함수를 호출하려면 `reasoning_effort: "none"`이 필요합니다. Astra의 함수 호출과 GPT-6의 `max`에는 [Responses](../responses/responses-reference)를 사용하세요.
</Note>
