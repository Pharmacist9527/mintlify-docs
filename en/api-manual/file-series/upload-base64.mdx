---
title: "Base64 File Upload"
openapi: '/en/api-manual/file-series/upload-base64.json POST /api/v1/files/upload/base64'
---

## Workflow

Use this endpoint when you have a small local file and want a hosted URL that can be reused in later AI requests.

1. Upload the local file as Base64.
2. Reuse the returned `file_url` or `download_url` in parameters such as `image_urls`, `audio_url`, `video_urls`, or Gemini `fileUri`.
3. Submit the AI request and poll the resulting `task_id` until the task is completed.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="bolt" href="/en/quickstart">
    Review the async task workflow used across EvoLink APIs.
  </Card>
  <Card title="Query Task Status" icon="chart-line" href="/en/api-manual/task-management/get-task-detail">
    Check task progress and retrieve final results with `task_id`.
  </Card>
  <Card title="Gemini Native API" icon="comments" href="/en/api-manual/language-series/gemini-3.0-pro/native-api/native-api-reference">
    See how hosted file URLs are reused in Gemini `fileUri` examples.
  </Card>
  <Card title="Video Inputs" icon="film" href="/en/api-manual/video-series/wan2.6/wan2.6-reference-video">
    See a representative media workflow that consumes uploaded file URLs.
  </Card>
</CardGroup>

<Tip>
For larger local media files, prefer the stream upload API. If your asset is already public, use URL upload instead.
</Tip>
