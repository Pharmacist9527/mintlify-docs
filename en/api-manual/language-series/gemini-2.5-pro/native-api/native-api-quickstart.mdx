---
title: "Gemini 2.5 Pro - Native API - Quick Start"
openapi: '/en/api-manual/language-series/gemini-2.5-pro/native-api/native-api-quickstart.json POST /v1beta/models/gemini-2.5-pro:generateContent'
---

<Tip>
**Streaming**: Replace `generateContent` with `streamGenerateContent` in the URL to enable streaming responses, receiving content in real-time chunks.
</Tip>

<Note>
**BaseURL**: The default BaseURL is `https://direct.evolink.ai`, which has better support for text models and long-lived connections. `https://api.evolink.ai` is the primary endpoint for multimodal services and serves as a fallback address for text models.
</Note>
