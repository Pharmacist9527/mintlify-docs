---
title: "GPT All-Model API - Chat Completions Quickstart"
openapi: '/en/api-manual/language-series/gpt/chat-completions/chat-completions-quickstart.json POST /v1/chat/completions'
---

<Note>
**BaseURL**: The default BaseURL is `https://direct.evolink.ai`, which has better support for text models and long-lived connections. `https://api.evolink.ai` is the primary endpoint for multimodal services and serves as a fallback address for text models.
</Note>

<Note>
**GPT-6**: Function calling with Sol / Luna requires `reasoning_effort: "none"` in this endpoint. Use the [Responses API](../responses/responses-reference) for Astra function calling or GPT-6 `max` effort.
</Note>
