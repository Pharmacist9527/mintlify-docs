---
title: "Interfaz de todos los modelos GPT - Inicio rápido de Chat Completions"
openapi: '/es/api-manual/language-series/gpt/chat-completions/chat-completions-quickstart.json POST /v1/chat/completions'
---

<Note>
**BaseURL**: La BaseURL predeterminada es `https://direct.evolink.ai`, que ofrece mejor compatibilidad con modelos de texto y admite conexiones persistentes. `https://api.evolink.ai` es el endpoint principal para servicios multimodales y actúa como dirección de respaldo para los modelos de texto.
</Note>

<Note>
**GPT-6**: Las llamadas a funciones con Sol / Luna requieren `reasoning_effort: "none"` en este endpoint. Use [Responses](../responses/responses-reference) para llamadas a funciones con Astra o el nivel `max` de GPT-6.
</Note>
